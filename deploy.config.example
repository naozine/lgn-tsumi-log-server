# -----------------------------------------------------------------------------
# Deploy Configuration Example
# -----------------------------------------------------------------------------
# このファイルを 'deploy.config' にリネームまたはコピーして使用してください。
# gitにはコミットしないでください (.gitignore推奨)。
#
# ※ このファイルは主に `make deploy` (バイナリ直接デプロイ) 用の設定です。
#    `make docker-deploy` を使う場合は、.env.production も作成してください。
# -----------------------------------------------------------------------------

# VPSのSSH接続ユーザー
VPS_USER = ubuntu

# VPSのIPアドレスまたはホスト名
VPS_HOST = 192.168.1.100

# SSHのポート番号 (デフォルト: 22)
SSH_PORT = 2222

# デプロイ先のディレクトリパス
VPS_DIR  = /home/ubuntu/apps/my-app

# -----------------------------------------------------------------------------
# バイナリ直接デプロイ (make deploy) 用の設定
# -----------------------------------------------------------------------------

# アプリケーションが使用するポート番号 (デフォルト: 8080)
# ※ docker-deploy では使用されません。.env.production の HOST_PORT を使用してください。
APP_PORT = 8080

# 初期管理者ユーザー (初回起動時にユーザーが0人の場合のみ作成される)
# ※ docker-deploy では .env.production の ADMIN_EMAIL/ADMIN_NAME を使用してください。
ADMIN_EMAIL = admin@example.com
ADMIN_NAME  = Administrator

# サーバーの公開アドレス (Magic Linkで使用)
# ※ docker-deploy では .env.production の SERVER_ADDR を使用してください。
SERVER_ADDR = https://example.com

# Systemdのサービス名 (ユーザーサービスとして登録される)
# ※ docker-deploy では使用されません。
SERVICE_NAME = my-app.service

# -----------------------------------------------------------------------------
# Docker デプロイ (make docker-deploy) 用の設定
# -----------------------------------------------------------------------------
# docker-deploy では、アプリの環境変数は .env.production で管理します。
# .env.production を作成し、以下のような内容を設定してください:
#
#   HOST_PORT=8080
#   SERVER_ADDR=https://example.com
#   ADMIN_EMAIL=admin@example.com
#   ADMIN_NAME=Administrator
#   SMTP_HOST=smtp.example.com
#   SMTP_PORT=587
#   ... etc
#
# デプロイ時に .env.production が VPS の .env として転送されます。

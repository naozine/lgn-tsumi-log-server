package components

import (
	"fmt"
	"time"
	"github.com/naozine/project_crud_with_auth_tmpl/internal/mdm"
)

// MDMDeviceList はMDMデバイス一覧を表示する
templ MDMDeviceList(devices []mdm.Device, cachedAt time.Time) {
	<div class="max-w-6xl mx-auto">
		<div class="mb-4">
			<a href="/mdm" class="text-sm font-medium text-gray-600 hover:text-gray-900">
				← MDM管理に戻る
			</a>
		</div>

		<div class="mb-8 flex items-center justify-between">
			<div>
				<h2 class="text-2xl font-bold tracking-tight text-gray-900">MDMデバイス一覧</h2>
				<p class="mt-1 text-sm text-gray-500">
					ManageEngine MDM管理下のデバイス一覧
				</p>
			</div>
			<div class="text-right text-sm text-gray-500">
				if !cachedAt.IsZero() {
					<p>最終更新: { cachedAt.Format("15:04:05") }</p>
				}
			</div>
		</div>

		if len(devices) == 0 {
			<div class="bg-white shadow sm:rounded-lg border border-gray-200">
				<div class="px-4 py-12 text-center">
					<p class="text-gray-500">登録されているデバイスはありません。</p>
				</div>
			</div>
		} else {
			<div class="bg-white shadow sm:rounded-lg border border-gray-200 overflow-hidden">
				<table class="min-w-full divide-y divide-gray-200">
					<thead class="bg-gray-50">
						<tr>
							<th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
								デバイス
							</th>
							<th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
								OS
							</th>
							<th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
								ユーザー
							</th>
							<th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
								シリアル番号
							</th>
						</tr>
					</thead>
					<tbody class="bg-white divide-y divide-gray-200">
						for _, device := range devices {
							<tr class="hover:bg-gray-50">
								<td class="px-4 py-4 whitespace-nowrap">
									<div class="flex items-center">
										<span class="text-xl mr-2">{ device.PlatformIcon() }</span>
										<div>
											<div class="text-sm font-medium text-gray-900">{ device.DeviceName }</div>
											if device.Model != "" {
												<div class="text-xs text-gray-500">{ device.Model }</div>
											}
										</div>
									</div>
								</td>
								<td class="px-4 py-4 whitespace-nowrap">
									<div class="text-sm text-gray-900">{ device.PlatformDisplayName() }</div>
									if device.OSVersion != "" {
										<div class="text-xs text-gray-500">{ device.OSVersion }</div>
									}
								</td>
								<td class="px-4 py-4 whitespace-nowrap">
									if device.User != nil {
										<div class="text-sm text-gray-900">{ device.User.UserName }</div>
										<div class="text-xs text-gray-500">{ device.User.UserEmail }</div>
									} else {
										<span class="text-sm text-gray-400">-</span>
									}
								</td>
								<td class="px-4 py-4 whitespace-nowrap">
									if device.SerialNumber != "" {
										<span class="text-sm font-mono text-gray-600">{ device.SerialNumber }</span>
									} else {
										<span class="text-sm text-gray-400">-</span>
									}
								</td>
							</tr>
						}
					</tbody>
				</table>
			</div>
			<div class="mt-4 text-sm text-gray-500 text-right">
				{ fmt.Sprintf("%d", len(devices)) } 台のデバイス
			</div>
		}

	</div>
}

// MDMTop はMDM管理のトップページを表示する
templ MDMTop() {
	<div class="max-w-3xl mx-auto">
		<div class="mb-8">
			<h2 class="text-2xl font-bold tracking-tight text-gray-900">MDM管理</h2>
			<p class="mt-1 text-sm text-gray-500">
				ManageEngine Mobile Device Manager Plus 連携機能
			</p>
		</div>

		<div class="grid gap-4">
			<a href="/mdm/devices" class="block bg-white shadow sm:rounded-lg border border-gray-200 hover:border-gray-300 hover:shadow-md transition-all">
				<div class="px-4 py-5 sm:p-6">
					<div class="flex items-center">
						<span class="text-3xl mr-4">📱</span>
						<div>
							<h3 class="text-lg font-medium text-gray-900">デバイス一覧</h3>
							<p class="mt-1 text-sm text-gray-500">MDM管理下のデバイスを確認する</p>
						</div>
					</div>
				</div>
			</a>
			<a href="/mdm/apps" class="block bg-white shadow sm:rounded-lg border border-gray-200 hover:border-gray-300 hover:shadow-md transition-all">
				<div class="px-4 py-5 sm:p-6">
					<div class="flex items-center">
						<span class="text-3xl mr-4">📦</span>
						<div>
							<h3 class="text-lg font-medium text-gray-900">アプリ一覧</h3>
							<p class="mt-1 text-sm text-gray-500">MDMに登録されたアプリを確認・配布する</p>
						</div>
					</div>
				</div>
			</a>
		</div>

		<div class="mt-6">
			<a href="/projects" class="text-sm font-medium text-gray-600 hover:text-gray-900">
				← プロジェクト一覧に戻る
			</a>
		</div>
	</div>
}

// MDMNotConfigured はMDMが設定されていない場合のメッセージを表示する
templ MDMNotConfigured() {
	<div class="max-w-3xl mx-auto">
		<div class="mb-8">
			<h2 class="text-2xl font-bold tracking-tight text-gray-900">MDM管理</h2>
		</div>

		<div class="bg-yellow-50 border border-yellow-200 rounded-lg p-6">
			<div class="flex">
				<div class="flex-shrink-0">
					<span class="text-2xl">⚠️</span>
				</div>
				<div class="ml-3">
					<h3 class="text-sm font-medium text-yellow-800">MDMが設定されていません</h3>
					<div class="mt-2 text-sm text-yellow-700">
						<p>MDM連携を使用するには、以下の環境変数を設定してください：</p>
						<ul class="list-disc list-inside mt-2 space-y-1">
							<li><code class="bg-yellow-100 px-1 rounded">ZOHO_CLIENT_ID</code></li>
							<li><code class="bg-yellow-100 px-1 rounded">ZOHO_CLIENT_SECRET</code></li>
							<li><code class="bg-yellow-100 px-1 rounded">ZOHO_REFRESH_TOKEN</code></li>
							<li><code class="bg-yellow-100 px-1 rounded">MDM_API_BASE_URL</code></li>
						</ul>
					</div>
				</div>
			</div>
		</div>

		<div class="mt-6">
			<a href="/projects" class="text-sm font-medium text-gray-600 hover:text-gray-900">
				← プロジェクト一覧に戻る
			</a>
		</div>
	</div>
}

// MDMError はMDM APIエラー時のメッセージを表示する
templ MDMError(errorMessage string) {
	<div class="max-w-3xl mx-auto">
		<div class="mb-8">
			<h2 class="text-2xl font-bold tracking-tight text-gray-900">MDMデバイス一覧</h2>
		</div>

		<div class="bg-red-50 border border-red-200 rounded-lg p-6">
			<div class="flex">
				<div class="flex-shrink-0">
					<span class="text-2xl">❌</span>
				</div>
				<div class="ml-3">
					<h3 class="text-sm font-medium text-red-800">MDM APIエラー</h3>
					<div class="mt-2 text-sm text-red-700">
						<p>デバイス一覧の取得に失敗しました。</p>
						<p class="mt-2 font-mono text-xs bg-red-100 p-2 rounded">{ errorMessage }</p>
					</div>
				</div>
			</div>
		</div>

		<div class="mt-6">
			<a href="/projects" class="text-sm font-medium text-gray-600 hover:text-gray-900">
				← プロジェクト一覧に戻る
			</a>
		</div>
	</div>
}
